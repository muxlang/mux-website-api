# Test-only dependencies (not shipped in the container image).
# Install alongside the runtime deps for local testing / CI:
#   uv pip install -r requirements.txt -r requirements-dev.txt
pytest>=9.1.1,<10.0
pytest-cov>=7.1.0,<8.0
ruff>=0.16.5,<1.0
mypy>=2.3.1,<3.0
pip-audit>=2.10.1,<3.0
